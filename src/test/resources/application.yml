spring:
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
    password:
    username: sa
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:datajpa
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create